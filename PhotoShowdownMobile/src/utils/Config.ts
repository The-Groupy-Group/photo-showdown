
const IP_ADDRESS = "192.168.1.15"; 

const PORT = "5299";

export const API_BASE = `http://${IP_ADDRESS}:${PORT}/api`;
export const WS_URL = `ws://${IP_ADDRESS}:${PORT}/api/ws`;
export const IMAGE_BASE = `http://${IP_ADDRESS}:${PORT}`;